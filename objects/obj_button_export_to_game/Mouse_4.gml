/// @description 
event_inherited();

//EXPORT TABLE
if (show_question("This exports all the hitboxes to the game, not to the save file. This may be catastrophic if done at the wrong point.\nCONTINUE?")) {
	if (!show_question("Press No to continue.")) {
		if (show_question("Final chance, Press Yes to continue.")) {
			show_debug_message("Good Luck. Exporting hitboxes to game")
			
			//ADD EXPORT SCRIPT
			//ADD EXPORT SCRIPT
		}
	}
}